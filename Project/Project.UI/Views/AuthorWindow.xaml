<Window x:Class="Project.UI.Views.AuthorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Author Editor" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="{Binding WindowTitle}" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>
            
            <!-- First Name -->
            <TextBlock Text="First Name *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="8" 
                     FontSize="14"
                     Margin="0,0,0,15"/>
            
            <!-- Last Name -->
            <TextBlock Text="Last Name *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="8" 
                     FontSize="14"
                     Margin="0,0,0,15"/>
            
            <!-- Birth Date -->
            <TextBlock Text="Birth Date *" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <DatePicker SelectedDate="{Binding BirthDay}" 
                        Padding="8"
                        FontSize="14"
                        Margin="0,0,0,25"/>
            
            <!-- Action Buttons -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0"
                        Content="Save"
                        Command="{Binding SaveCommand}"
                        Background="#91FFA0"
                        BorderThickness="0"
                        Padding="12"
                        FontSize="14"
                        FontWeight="Bold"
                        Cursor="Hand"/>
                
                <Button Grid.Column="2"
                        Content="Cancel"
                        Command="{Binding CancelCommand}"
                        Background="#FFB0B0"
                        BorderThickness="0"
                        Padding="12"
                        FontSize="14"
                        FontWeight="Bold"
                        Cursor="Hand"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
