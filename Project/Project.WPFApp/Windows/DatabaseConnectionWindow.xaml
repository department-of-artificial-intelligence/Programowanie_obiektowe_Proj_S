<Window x:Class="Project.WPFApp.Windows.DatabaseConnectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Project.WPFApp.Windows"
        xmlns:vm="clr-namespace:Project.WPFApp.ViewModels"
        mc:Ignorable="d"
        Title="SQL Server" Height="240" Width="360" MinHeight="100" MinWidth="240" MaxHeight="720" MaxWidth="1024">

    <Window.DataContext>
        <vm:DatabaseConnectionViewModel />
    </Window.DataContext>

    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabControl.Resources>
                <Style TargetType="Grid">
                    <Setter Property="Margin" Value="4,4,4,8" />
                </Style>
            </TabControl.Resources>
            
            <TabItem Header="By Credentials">
                <ScrollViewer>
                    <StackPanel Orientation="Vertical">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Server Name" />
                            <TextBox Grid.Column="1" Text="{Binding ServerName}" IsEnabled="{Binding IsCanBeConnected}" />
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Database Name" />
                            <TextBox Grid.Column="1" Text="{Binding DatabaseName}" IsEnabled="{Binding IsCanBeConnected}" />
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Parameters" />
                            <TextBox Grid.Column="1" Text="{Binding Parameters}" IsEnabled="{Binding IsCanBeConnected}" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            
            <TabItem Header="By Connection String">
                <StackPanel Orientation="Vertical">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Connection String" />
                        <TextBox Grid.Column="1" Text="{Binding ConnectionString}" IsEnabled="{Binding IsCanBeConnected}" />
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <CheckBox Margin="4" Grid.Column="0" VerticalAlignment="Center" Content="Save connection string" IsEnabled="{Binding IsCanBeConnected}" />
            <Button Margin="4" Grid.Column="1" Content="Connect" Command="{Binding ConnectCommand}" IsEnabled="{Binding IsCanBeConnected}" />
        </Grid>
    </Grid>
</Window>
