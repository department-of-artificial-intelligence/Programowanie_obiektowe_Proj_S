<Window x:Class="Project.WPFApp.Windows.CreateRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Project.WPFApp.Windows"
        xmlns:vm="clr-namespace:Project.WPFApp.ViewModels"
        mc:Ignorable="d"
        Title="Create Room" Height="175" Width="300">
    
    <Window.DataContext>
        <vm:CreateRoomViewModel />
    </Window.DataContext>
    
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="32" />
        </Grid.RowDefinitions>

        <ScrollViewer>
            <ScrollViewer.Resources>
                <Style TargetType="Grid">
                    <Setter Property="Margin" Value="4,4,4,8" />
                </Style>
            </ScrollViewer.Resources>

            <StackPanel Orientation="Vertical">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Number" />
                    <TextBox Grid.Column="1" Text="{Binding Number}" />
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Floor" />
                    <TextBox Grid.Column="1" Text="{Binding Floor}" />
                </Grid>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Price per Day" />
                    <TextBox Grid.Column="1" Text="{Binding PricePerDay}" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Button Margin="4" Grid.Column="1" Content="Create" Command="{Binding CreateRoomCommand}" />
        </Grid>
    </Grid>
</Window>
