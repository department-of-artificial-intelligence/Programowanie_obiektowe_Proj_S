namespace Project.Model
{
    public enum PerformanceStatus
    {
        Scheduled,
        Canceled,
        Finished
    }

    public enum TicketStatus
    {
        Available,
        Reserved,
        Sold
    }
}